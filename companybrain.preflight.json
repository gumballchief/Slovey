{
  "requiredChecks": ["typecheck", "test", "build", "decision-check"],
  "optionalChecks": ["secret-scan", "lint", "format", "env-check", "route-check", "deps"],
  "maxAttempts": 5,
  "blockCommitOnFailure": true,
  "allowSkippedChecks": false,
  "timeoutMs": 600000,
  "commands": {
    "typecheck": "pnpm typecheck",
    "test": "pnpm test",
    "build": "pnpm build"
  }
}
