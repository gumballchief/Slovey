# dependencies
node_modules/
.pnpm-store/

# builds
dist/
.next/
out/
*.tsbuildinfo

# env / secrets — ignore every .env variant except the committed template
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
# build-output copies of env (Next standalone) — belt & suspenders
**/.next/**/.env

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# os / editor
.DS_Store
Thumbs.db

# drizzle
packages/db/drizzle/meta/_journal.bak
Company Brain.html
