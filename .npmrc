# Flat node_modules (npm-like) for maximum compatibility with the Next.js + three.js
# frontend, which expects hoisted dependencies.
node-linker=hoisted
# Don't trigger an implicit reinstall before every filtered script run.
verify-deps-before-run=false
